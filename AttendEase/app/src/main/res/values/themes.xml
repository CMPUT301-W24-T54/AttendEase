<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AttendEase" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.AttendEase" parent="Base.Theme.AttendEase">
    <item name="colorPrimary">@color/black</item>
    <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.App" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        ...
        <item name="bottomNavigationStyle">@style/Widget.Material3.BottomNavigationView.ActiveIndicator</item>
        <item name="colorPrimary">@color/transparent_blue</item>
        <item name="colorOnPrimary">@color/transparent_blue</item>
    </style>

</resources>